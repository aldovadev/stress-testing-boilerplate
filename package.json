{
  "scripts": {
    "k6:vcontent": "docker run --rm --add-host=host.docker.internal:host-gateway --env-file env/local.env -e VUS=20 -e DURATION=30s -v $(pwd):/app -w /app grafana/k6 run scripts/main.js"
  }
}